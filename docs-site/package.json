{
  "name": "react-native-mock-interviews",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate:volunteers": "node ./scripts/generate-volunteer-docs.mjs",
    "docusaurus": "docusaurus",
    "prestart": "npm run generate:volunteers",
    "start": "docusaurus start",
    "prebuild": "npm run generate:volunteers",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "predeploy": "npm run generate:volunteers",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "build-storybook": "build-storybook",
    "dev-storybook": "start-storybook -p 6006"
  },
  "dependencies": {
    "@docusaurus/core": "^3.10.1",
    "@docusaurus/preset-classic": "^3.10.1",
    "@easyops-cn/docusaurus-search-local": "^0.55.2",
    "@mdx-js/react": "^3.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.10.1",
    "@docusaurus/types": "^3.10.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
